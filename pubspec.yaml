name: ar_sns_frontend
description: "AR SNS Frontend Flutter プロジェクト"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: "^3.7.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  js: ^0.6.5 
  cupertino_icons: ^1.0.8
  firebase_auth: ^4.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
